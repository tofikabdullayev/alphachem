import React from 'react';
import Layout from '../components/Layout';

export interface SliderProps {}

const SliderPage: React.FC<SliderProps> = () => {
  return <Layout>Slider page</Layout>;
};

export default SliderPage;
